{
  "name": "homepage",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.html",
    "build": "rm -rf build && bun run build:posts && bun build src/index.html --outdir build --minify --target browser --define.process.env.NODE_ENV=production",
    "build:posts": "bun run scripts/build-posts.ts",
    "serve": "bun run scripts/serve.ts",
    "deploy": "bun run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  }
}
